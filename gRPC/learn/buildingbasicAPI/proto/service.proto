syntax = "proto3";
option go_package = "./";
package proto;

message Request {
	int64 id = 1;
	int64 name = 2;
}

message Response {
	int64 result = 1;
}

service AddService {
	rpc Add(Request) returns (Response);
	rpc Multiply(Request) returns (Response);
}
